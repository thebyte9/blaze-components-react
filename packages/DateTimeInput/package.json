{
  "name": "@blaze-react/date-time-input",
  "version": "0.6.1",
  "description": "",
  "main": "./dist/main.js",
  "scripts": {
    "build": "NODE_ENV=production webpack --config webpack.config.js",
    "build-storybook": "build-storybook -c .storybook -o docs-build",
    "lint": "eslint '*/**/*.{ts,tsx}' --ignore-path ../../.eslintignore --quiet --fix",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "pre-publish": "yarn build && yarn lint && yarn test"
  },
  "keywords": [],
  "author": "byte9",
  "license": "MIT",
  "devDependencies": {
    "@babel/cli": "^7.13.14",
    "@babel/core": "^7.13.14",
    "@types/react-datepicker": "^3.1.8"
  },
  "dependencies": {
    "react-datepicker": "^3.7.0",
    "webpack": "^5.28.0",
    "webpack-cli": "^4.6.0"
  }
}
